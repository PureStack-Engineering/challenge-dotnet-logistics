- name: 🔍 Audit .NET Structure
        run: |
          echo "Checking Critical Files..."
          if [ ! -f "src/PureStackLogistics/Program.cs" ]; then echo "❌ Missing Program.cs"; exit 1; fi
          if [ ! -f "src/PureStackLogistics/Services/InventoryService.cs" ]; then echo "❌ Missing InventoryService.cs"; exit 1; fi
          if [ ! -f "tests/PureStackLogistics.Tests/InventoryTests.cs" ]; then echo "❌ Missing Tests"; exit 1; fi
          echo "✅ .NET Project Structure Validated."
