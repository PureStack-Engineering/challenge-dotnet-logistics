name: PureStack Audit System

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  audit-dotnet:
    runs-on: ubuntu-latest
    name: 👮 PureStack Quality Gate
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔍 Phase 1 Structural Integrity
        run: |
          echo "::group::File Validation"
          if [ ! -f "PureStackLogistics.sln" ]; then echo "❌ Missing Solution File"; exit 1; fi
          if [ ! -f "src/PureStackLogistics/Services/InventoryService.cs" ]; then echo "❌ Missing InventoryService.cs"; exit 1; fi
          echo "✅ Project Structure seems valid."
          echo "::endgroup::"

      - name: 🔧 Setup .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: 📦 Restore Dependencies
        run: dotnet restore

      - name: 🕵️ Phase 2 Anti-Pattern Detection
        run: |
          echo "::group::Static Analysis"
          if grep -r --include="*.cs" "\.Result" src/; then echo "❌ CRITICAL: Blocking call .Result detected"; exit 1; fi
          if grep -r --include="*.cs" "\.Wait(" src/; then echo "❌ CRITICAL: Blocking call .Wait() detected"; exit 1; fi
          if grep -r --include="*.cs" "Thread.Sleep" src/; then echo "❌ CRITICAL: Thread.Sleep detected"; exit 1; fi
          echo "✅ No major anti-patterns detected."
          echo "::endgroup::"

      - name: 🏗️ Phase 3 Architecture Scout
        run: |
          echo "::group::Architecture Scan"
          INTERFACE_COUNT=$(find src/PureStackLogistics -name "I*Repository.cs" | wc -l)
          if [ "$INTERFACE_COUNT" -gt 0 ]; then echo "🏆 PRO LEVEL: $INTERFACE_COUNT Interface(s) found."; fi
          echo "::endgroup::"

      - name: 🏗️ Build Project
        run: dotnet build --no-restore

      - name: 🧪 Phase 4 Functional Validation
        run: dotnet test --no-build --verbosity normal
